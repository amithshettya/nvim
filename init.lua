-- set the stage with nvim global config
require("options")

-- decorate the stage by installing all the plugins
require("lazy_plugins")

-- the stage is ready now lets add my keybindings, this is my cheatsheet!!
require("keybinding")
